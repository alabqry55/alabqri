{
  "name": "alabqri-site",
  "version": "1.0.0",
  "private": true,
  "description": "منصة العبقري - Cloudflare Pages + Functions + D1",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name alabqri-pro",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  },
  "dependencies": {
    "tailwindcss": "^3.4.14"
  }
}
